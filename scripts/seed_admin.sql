-- Seed inicial do usuário admin para Materiais Opme Backend
-- Gere o SQL com:
--   php scripts/create_admin_user.php --username=admin --password='SENHA_FORTE' --full-name='Administrador do Sistema' > /tmp/admin.sql
-- Depois execute:
--   mysql < /tmp/admin.sql

USE materiais_opme;

-- Exemplo de estrutura esperada pelo gerador:
-- INSERT INTO users (username, password_hash, full_name, role, active, created_at, updated_at)
-- VALUES (...)
-- ON DUPLICATE KEY UPDATE ...;
